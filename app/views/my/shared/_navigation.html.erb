<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <a class="navbar-brand" href="#"><%=t(:gugnn_member)%></a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a data-target="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%=@user.nickname%> <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%=link_to t(:change_password), change_password_my_account_path(@user)%></li>
            <li><%=link_to t(:change_profile), change_profile_my_account_path(@user)%></li>
            <li><%=link_to t(:sign_out), destroy_user_session_path, method: :delete%></li>
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
