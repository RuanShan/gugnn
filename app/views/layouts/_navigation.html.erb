<%# navigation styled for Bootstrap 3.0 %>
<nav class="">
  <div class="container">
    <div class=" ">
      <ul class="nav navbar-nav">
        <li> <%=session[:city]%><%= link_to t(:change_city), select_city_path %> </li>
      </ul>

      <ul class="nav navbar-nav pull-right">
        <% if user_signed_in? %>
          <li class="dropdown">
            <%=link_to nil, class:"dropdown-toggle", "data-target":"#",  "data-toggle":"dropdown", role:"button", "aria-haspopup":"true", "aria-expanded":"false" do %>
              <%=current_user.nickname || '昵称'%> <span class="caret"></span>
            <% end %>
            <ul class="dropdown-menu">
              <li><%=link_to t(:change_profile), change_profile_my_account_path()%></li>
              <li><%=link_to t(:change_password), change_password_my_account_path()%></li>
              <li><%=link_to t(:sign_out), destroy_user_session_path, method: :delete%></li>
            </ul>
          </li>
          <li><%= link_to '个人中心', my_account_path %></li>
          <li><%= link_to '退出', destroy_user_session_path, :method=>'delete' %></li>
        <% else %>
          <li><%= link_to '登陆', new_user_session_path %></li>
          <li><%= link_to '注册', new_user_registration_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
